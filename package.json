{
	"name": "music-theory-app",
	"version": "1.0.0",
	"description": "Root of the music-theory-app monorepo. This package.json only owns repo-wide git tooling (Husky + lint-staged); each service (e.g. backend/) is its own independent npm project with its own dependencies.",
	"private": true,
	"scripts": {
		"prepare": "husky"
	},
	"lint-staged": {
		"*.{json,yml,yaml,md}": ["prettier --check"]
	},
	"devDependencies": {
		"husky": "^9.1.7",
		"lint-staged": "^17.0.7",
		"prettier": "^3.8.3"
	}
}
